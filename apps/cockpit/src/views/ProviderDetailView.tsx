export function ProviderDetailView({ id }: { id: string }) {
  return <div className="flex flex-1 items-center justify-center text-[13px] text-muted-foreground">ProviderDetail {id} — coming up.</div>;
}

# @ryuzi/mission-control (planned)

Web app to remotely view and control **all** workdirs and sessions across gateways.

Will connect to the `ryuzi` daemon (Rust CLI/core) over the `ControlPlaneApi` (WebSocket transport via `Bun.serve()`). Not implemented yet — reserved as a workspace so the name and seam exist.

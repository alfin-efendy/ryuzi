# @harness/mission-control (planned)

Web app to remotely view and control **all** workdirs and sessions across gateways.

Will connect to the `hr` daemon (`@harness/cli`) over the `ControlPlaneApi` (WebSocket transport via `Bun.serve()`) and consume `@harness/protocol` types. Not implemented yet — reserved as a workspace so the name and seam exist.

contract = 1
id = "vercel-sandbox"
name = "Vercel Sandbox"
version = "0.1.0"
publisher = "Vercel (no MCP surface)"
description = "Docs-only entry: Vercel Sandbox has NO MCP surface at all — it's SDK/CLI-only, and it isn't exposed via Vercel's own MCP tool server (the `vercel` catalog entry) either. Use the `daytona` or `e2b` catalog entries for sandboxed code execution via MCP instead."
homepage = "https://vercel.com/docs/vercel-sandbox"
icon = "box"
categories = ["sandbox"]
verified = false
experimental = true

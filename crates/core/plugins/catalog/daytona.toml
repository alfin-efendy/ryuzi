contract = 1
id = "daytona"
name = "Daytona"
version = "0.1.0"
publisher = "Daytona (official)"
description = "Cloud dev-sandbox creation and control via Daytona's official CLI-hosted MCP server (`daytona mcp start`). Requires the `daytona` CLI installed locally (`brew install daytonaio/cli/daytona`) and an active `daytona login` session — auth is the CLI's own login session, not a portable secret ryuzi stores."
homepage = "https://www.daytona.io/docs"
icon = "box"
categories = ["sandbox"]
verified = true

[[mcp]]
name = "daytona"
transport = "stdio"
command = "daytona"
args = ["mcp", "start"]

[menu]
section = "plugins"
label = "Daytona"

//! Shared request/response shapes for the RPC command families under
//! `api/`. Empty for now — populated as later tasks add command families
//! that need types beyond the engine's own domain/store types.
